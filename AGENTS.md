# 작업 방식

- 요청받은 변경을 완료하면 관련 검증 후 현재 작업 브랜치에 커밋하고 기존 GitHub 원격 저장소까지 바로 업로드한다. 사용자가 정한 작업 원칙이므로 업로드만을 위한 별도 요청을 기다리지 않는다.
- 완료 답변에는 원격 반영을 확인한 커밋 링크와 검증 결과, 아직 확인하지 못한 사항을 간단히 알린다.

## 이미지 제작 규칙

- 앞으로 프로젝트용 이미지는 반드시 실제 알파 채널이 있는 투명 PNG로 생성한다. 생성 프롬프트에도 투명 배경을 필수 조건으로 명시한다.
- 체크무늬를 그림에 넣거나 흰색·초록색 등 단색 배경을 깔아 투명 이미지를 대신하지 않는다. 게임에서 배경색을 제거한다는 이유로 불투명 원본을 납품하지 않는다.
- 게임 적용과 전달 전에 파일의 알파 채널 및 실제 투명 픽셀을 검사한다. 테두리와 무기 손잡이 등의 빈 구멍도 확인한다. 불투명 결과물은 완료본으로 취급하지 않는다.
- 이미지·음원은 Git에 직접 올리지 않고 사용자가 업로드할 수 있도록 ZIP으로 전달한다. 코드 변경은 위 작업 방식대로 검증 후 업로드한다.
