# 몬스터의 테마와 Call & Response 계획

샘플 몬스터는 **9종·18개 패턴**이다. 각 몬스터는 패턴 2개를 가지며 각 패턴이 곡의 슬롯에 독립적으로 배치된다. 준비 화면에서는 실제로 공격이 남은 몬스터의 두 패턴과 각각의 등장 횟수를 보여준다. 곡 전체의 공격 위치는 공개하지 않는다.

## 몬스터 테마

Tap을 메인으로 하는 몬스터가 **5종**이다. 나머지 Hold·Dive·Shake·Flick은 한 종씩 있다. 반짝 박쥐를 제외하면 두 패턴 모두 해당 몬스터의 메인 입력만 요구한다.

| 몬스터 | 메인 입력 | 첫 패턴 | 둘째 패턴 | 선택 가중치 |
|---|---|---|---|---:|
| 통통 슬라임 | Tap | 넷에 통: Call–Call–Call–Tap | 트레실로 예고: Call–Call–Tap, 3+3+2 | 1.4 |
| 행진 슬라임 | Tap | 한 박 간격으로 세 번 Tap | 한 박 쉬어 가며 두 번 Tap | 1.4 |
| 춤추는 박쥐 | Tap | 트레실로 Tap, 3+3+2 | 간격을 바꾼 Tap, 3+2+3 | 1.4 |
| 뒷박 도깨비 | Tap | Call 반 박 뒤 Tap | 두 번의 Call 후 엇박 Tap 두 번 | 1.4 |
| 반짝 박쥐 | Tap 중심 혼합 | 반 박 간격 Tap 두 번 | 한 박 Hold | 0.35 |
| 철갑 거북 | Hold | 두 번의 Call 후 두 박 Hold | 한 번의 Call 후 한 박 Hold | 1 |
| 잠수 가오리 | Dive | 네 박 잠수 후 떼기 | 두 박 잠수 후 떼기 | 1 |
| 방울 정령 | Shake | 한 박 안에 한 번 왕복 | 한 박 휴식을 사이에 둔 두 번의 왕복 | 1 |
| 튕김 도깨비 | Flick | 두 번의 Call 다음 박에 Flick | 두 번째 Call에서 반 박 뒤 Flick | 1 |

선택 가중치는 곡에 맞는 몬스터 사이의 상대 비중이다. 패턴의 배치 확률과는 별도다. 입력 종류가 크게 바뀌는 반짝 박쥐는 낮은 가중치로 드물게 등장한다. 현재 아트는 기본 6종을 사용하며 행진 슬라임·춤추는 박쥐·뒷박 도깨비는 각각 기존 슬라임·박쥐·도깨비 이미지에 다른 색을 적용한다. 패턴, 이름과 인스턴스는 독립적이다.

## 트레실로와 Call

1박은 4틱이며 4/4 한 마디는 16틱이다. 트레실로는 셋잇단음표나 5/4로 만들지 않는다. 한 구절의 타격 간격은 **6틱 + 6틱 + 4틱**, 즉 **1.5박 + 1.5박 + 1박**이다.

- 통통 슬라임의 트레실로 예고는 구절의 1박·2박 반에 Call, 4박에 Tap이다. Call 상대 위치 0·6틱, Response 시작까지 12틱이다.
- 넷에 통은 같은 길이에 Call 상대 위치 0·4·8틱을 사용하고 12틱에서 Tap한다.
- 춤추는 박쥐는 한 박 전조 후 Response 구간의 0·6·12틱에서 세 번 Tap한다. 둘째 패턴은 0·6·10틱으로 바꾼 3+2+3 간격이다.
- 샘플의 첫 Call은 정박에 맞춘다. 뒷박 도깨비는 Call 선행 시간이 2틱 또는 6틱이므로 Response가 실제 곡의 반 박 위치에 들어간다. 맞는 슬롯이 없으면 그 패턴은 해당 곡에 배치하지 않는다.

Call은 플레이어 입력이나 피해 판정이 아니다. 전투 모션, 금빛 신호, 문자와 합성 Call 소리가 동일한 절대 시점을 사용한다. Call 소리는 배경 박자음과 다른 길이·음높이를 쓰고, 혼합 몬스터의 Tap/Hold 예고도 음높이를 구분한다. 실제 음원이나 녹음된 몬스터 대사는 아직 없다.

## 배치와 충돌

1. 패턴 전체, Response 구절 길이, Call 선행 시간과 정박 정렬을 만족하는 후보가 있는 몬스터를 추린다. 음악 슬롯은 바꾸지 않는다.
2. 상대 선택 가중치로 중복 없이 추첨한다. 일반·엘리트·보스 모두 1필드는 최대 1종, 2필드는 2종, 3필드부터 최대 3종이다.
3. 각 패턴은 별도의 난수로 후보를 시간순으로 훑는다. 기본 참여 확률 `p`, 슬롯 가중치 `w`에 대해 `1 - (1 - p)^w`로 배치한다. 형제 패턴이나 다른 몬스터의 배치는 보지 않는다.
4. 한 패턴 내 반복은 자신의 Response와 휴식을 지킨다. 모든 후보를 건너뛴 패턴에는 가능한 후보 하나를 가중 추첨으로 제출한다. 이 보장은 충돌 중재 전의 제출에만 적용된다.
5. 제출안을 모두 모아 가장 이른 충돌부터 해결한다. 충돌한 묶음의 Call과 모든 Response를 함께 철회하며 재삽입하지 않는다. 공격이 전부 사라진 몬스터는 최종 계획에서 빠진다.

**같은 몬스터의 패턴은 같은 종류의 입력이어도 겹칠 수 없다.** 자기 Call/Response 중에 다른 패턴의 Call이 시작되거나, 앞 패턴의 휴식이 끝나기 전에 새 Response를 요구하면 충돌한다. 다음 Call이 앞 패턴의 휴식을 활용하는 기존 규칙은 유지한다. 자기 충돌은 시드 기반 추첨으로 한쪽 묶음을 제거한다.

서로 다른 몬스터는 겹친 입력 종류가 다르면 충돌한다. 같은 종류도 누름/떼기와 유지 조건이 호환되어야 공유할 수 있다. 같은 종류의 두 Tap처럼 공유 가능한 입력과 서로 다른 몬스터의 Call 중첩은 허용한다. 한 패턴 안에 직접 작성한 복합 손동작은 `InputCompatibility.IsPlayable`로 따로 검증한다.

다른 몬스터끼리 충돌할 때는 **현재 점유 박자가 많은 몬스터가 해당 묶음을 양보**한다. 철회할 때마다 남은 점유량을 다시 계산하며 같으면 시드로 추첨한다. 점유량은 곡의 입력 위치 합집합이고 Call·빈 슬롯 선택지·공격 개수는 포함하지 않는다. Shake는 실제 왕복을 일찍 끝낼 수 있지만 배치에서는 지정된 구간 전체를 보호한다.

추첨·패턴 배치·동률 처리는 런/노드에서 파생한 난수로 재현된다. 패턴 ID를 기준으로 난수를 분리하므로 형제 패턴의 순서나 존재가 다른 패턴의 제출안에 영향을 주지 않는다.

## 데이터와 화면

| API | 역할 |
|---|---|
| `MonsterDefinition.Patterns` | 몬스터에 속한 패턴 정의 목록 |
| `MonsterPatternDefinition` | 패턴 이름·설명·리듬·Call·Response 길이·휴식·배치 확률·Call 정렬 |
| `MonsterDefinition.MainGesture / EncounterWeight / ArtId` | 메인 입력·선택 비중·재사용하는 이미지 |
| `MonsterProposal.Placements` | 두 패턴이 독립적으로 제출한 후보 묶음. 자기 중첩을 포함할 수 있음 |
| `PlannedAttack.Pattern / Placement` | 이 공격이 사용하는 정확한 패턴 정의와 실제 곡 슬롯 |
| `BattlePlanner.Conflicts` | 자기 Call/휴식 충돌 또는 다른 몬스터의 입력 충돌 |
| `PlanWithdrawal.IsSelfConflict` | 자기 패턴끼리의 충돌로 철회했는지 여부 |
| `Session.BattlePlan.Monsters / Attacks / Calls` | 최종 몬스터·공격·시간순 전조 |

공격 ID는 몬스터 인스턴스·패턴 ID·Response 시작점을 모두 포함한다. 같은 시점에 제출된 두 패턴도 서로 다른 ID를 가진다. `RhythmRound`는 몬스터의 첫 패턴이 아니라 각 공격의 실제 패턴에서 판정 노트를 만든다.

`MonsterPatternView`는 몬스터 카드 안에 두 패턴을 각각 표시하며 첫 Call을 1박으로 안내한다. `MonsterPatternGraphic`과 연주 메뉴는 현재 공격의 패턴에 맞춰 입력 레인·Call·Response·휴식 길이와 결과를 전환한다. 준비로 돌아가거나 메뉴를 열어도 계획을 다시 추첨하지 않는다.

## 검증

핵심 테스트 **86개 통과**. 기존 맵·배치·판정·피격 검증에 더해 18개 패턴의 실제 곡 배치 가능성, Tap 등장 비중, 혼합 몬스터의 희소성, 패턴별 독립 난수, 자기 Call/Response/휴식 중첩, 다른 몬스터의 같은 입력 공유, 트레실로 타격 위치와 실제 판정, 정박 Call/엇박 Response를 확인한다.

C# 39개 파일 구문 검사도 통과했다. Unity Editor가 없는 환경이므로 Unity 컴파일·PlayMode·실제 화면과 음향 확인은 남아 있다. `RunMap`에서 두 패턴의 전환, Call 소리, Shake 한 번 왕복과 일시정지/재개를 확인한다. 플레이어 피격과 회차 수명은 [RhythmPlayback.md](RhythmPlayback.md)에 있다.
